Hello <%= @user.name %>

At <%= @user.reset_sent_at.to_formatted_s(:long) %>, you requested to reset your password.

Click the link below to proceed with resetting your password:

  <%= edit_password_reset_url(@user.reset_token, email: @user.email) %>

This password reset link will expire in 2 hours.
